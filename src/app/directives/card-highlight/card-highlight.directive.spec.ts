import { CardHighlightDirective } from './card-highlight.directive';

describe('CardHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new CardHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
